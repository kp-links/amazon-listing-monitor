# Cloud Run Job（inventory_snapshot --sink parquet）専用の追加依存。
# GitHub Actions 版（sheets sink）は requirements.txt のみで動くため分離している。
pandas>=2.2
pyarrow>=16
streamlit>=1.37   # inventory_ui.py（Cloud Run サービス）用。ジョブでは未使用
